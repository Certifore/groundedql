version: 1
name: GroundedQL QueryPlan Spec (auto-generated)
description: Auto-generated from schema.yaml. Instructions for generating QueryPlan
  JSON for the GroundedQL compiler. Output MUST be valid JSON only (no markdown, no
  prose, no SQL).
system_instructions: 'You are a QueryPlan generator.

  Your job is to convert a natural-language question into a JSON QueryPlan object.

  You MUST output JSON only — no markdown, no prose, no SQL.



  DATA CONTEXT:

  BIRD Mini-Dev database: california_schools

  If the user mentions the organization/campus/site by name, that refers to the ENTIRE
  dataset — do NOT add a filter for it.


  RULES:

  - dataset MUST be one of: "frpm", "satscores", "schools"

  - Use ONLY the logical column names listed in schema_summary below.

  - Do NOT use physical DB column names or write SQL.

  - Always include limit and offset.

  - Give every dimension and metric an alias.

  - metric aliases must be unique within the plan.

  - Prefer consistent, deterministic plans: same question → same JSON.

  '
defaults:
  limit: 100
  offset: 0
  max_limit: 1000
operators_supported:
  comparison:
  - '='
  - '!='
  - '>'
  - '>='
  - <
  - <=
  membership:
  - in
  - not_in
  text:
  - contains
  - not_contains
  - starts_with
  - ends_with
  null_checks:
  - is_null
  - is_not_null
schema_summary:
  frpm:
    columns:
    - name: cdscode
      type: text
    - name: academic_year
      type: text
    - name: county_code
      type: text
    - name: district_code
      type: bigint
    - name: school_code
      type: text
    - name: county_name
      type: text
    - name: district_name
      type: text
    - name: school_name
      type: text
    - name: district_type
      type: text
    - name: school_type
      type: text
    - name: educational_option_type
      type: text
    - name: nslp_provision_status
      type: text
    - name: charter_school_(y/n)
      type: bigint
    - name: charter_school_number
      type: text
    - name: charter_funding_type
      type: text
    - name: irc
      type: bigint
    - name: low_grade
      type: text
    - name: high_grade
      type: text
    - name: enrollment_(k_12)
      type: float
    - name: free_meal_count_(k_12)
      type: float
    - name: percent_(%)_eligible_free_(k_12)
      type: float
    - name: frpm_count_(k_12)
      type: float
    - name: percent_(%)_eligible_frpm_(k_12)
      type: float
    - name: enrollment_(ages_5_17)
      type: float
    - name: free_meal_count_(ages_5_17)
      type: float
    - name: percent_(%)_eligible_free_(ages_5_17)
      type: float
    - name: frpm_count_(ages_5_17)
      type: float
    - name: percent_(%)_eligible_frpm_(ages_5_17)
      type: float
    - name: 2013_14_calpads_fall_1_certification_status
      type: bigint
  satscores:
    primary_id: cds
    primary_id_note: Use count_distinct('cds') when counting distinct satscores.
    columns:
    - name: cds
      type: text
    - name: rtype
      type: text
    - name: sname
      type: text
    - name: dname
      type: text
    - name: cname
      type: text
    - name: enroll12
      type: bigint
    - name: numtsttakr
      type: bigint
    - name: avgscrread
      type: bigint
    - name: avgscrmath
      type: bigint
    - name: avgscrwrite
      type: bigint
    - name: numge1500
      type: bigint
  schools:
    columns:
    - name: cdscode
      type: text
    - name: ncesdist
      type: text
    - name: ncesschool
      type: text
    - name: statustype
      type: text
    - name: county
      type: text
    - name: district
      type: text
    - name: school
      type: text
    - name: street
      type: text
    - name: streetabr
      type: text
    - name: city
      type: text
    - name: zip
      type: text
    - name: state
      type: text
    - name: mailstreet
      type: text
    - name: mailstrabr
      type: text
    - name: mailcity
      type: text
    - name: mailzip
      type: text
    - name: mailstate
      type: text
    - name: phone
      type: text
    - name: ext
      type: text
    - name: website
      type: text
    - name: opendate
      type: date
    - name: closeddate
      type: date
    - name: charter
      type: bigint
    - name: charternum
      type: text
    - name: fundingtype
      type: text
    - name: doc
      type: text
    - name: doctype
      type: text
    - name: soc
      type: text
    - name: soctype
      type: text
    - name: edopscode
      type: text
    - name: edopsname
      type: text
    - name: eilcode
      type: text
    - name: eilname
      type: text
    - name: gsoffered
      type: text
    - name: gsserved
      type: text
    - name: virtual
      type: text
    - name: magnet
      type: bigint
    - name: latitude
      type: float
    - name: longitude
      type: float
    - name: admfname1
      type: text
    - name: admlname1
      type: text
    - name: admemail1
      type: text
    - name: admfname2
      type: text
    - name: admlname2
      type: text
    - name: admemail2
      type: text
    - name: admfname3
      type: text
    - name: admlname3
      type: text
    - name: admemail3
      type: text
    - name: lastupdate
      type: date
structural_invariants: "DATASET: must be a logical table name from schema_summary.\n\
  \nDIMENSIONS: list of {field, alias} — alias is REQUIRED.\n\nMETRICS: list of {agg,\
  \ field, alias} — alias is REQUIRED and unique.\n  Allowed agg: count, count_distinct,\
  \ sum, avg, min, max.\n  count: field may be '*' or a logical column.\n  count_distinct/sum/avg/min/max:\
  \ field MUST be a logical column, NOT '*'.\n\nFILTERS: list of {field, op, value}.\n\
  \  'contains' = case-insensitive substring match (ILIKE %value%).\n\nORDER_BY: must\
  \ reference an existing dimension alias or metric alias.\n\nLIMIT/OFFSET: always\
  \ present. For rollup inner queries, omit limit so all rows are included before\
  \ outer aggregation.\n\nROLLUP: if present, rollup.metrics[*].field MUST reference\
  \ a metric alias or dimension alias from the inner plan — NOT a raw dataset column\
  \ name.\n\n\n--- READ SQL SURFACE (compiler contract) ---\nREAD_SQL_SURFACE_VERSION=1\
  \ (internal contract).\nThe compiler targets read-only SELECT-shaped plans over\
  \ schema.yaml only.\nComposition: single_select, with_cte_recursive_false, set_ops_union_union_all_intersect_except.\n\
  FROM / joins: single_dataset, explicit_joins_inner_left, link_declared_joins, self_join_via_join_as.\n\
  Predicates: legacy_filters_and, where_boolean_tree_and_or_not_cmp, relative_date_sentinels.\n\
  Aggregation: group_by, having, rollup_outer_metrics, filtered_aggregates_via_expr.\n\
  Windows: func_over_partition_order.\nSubqueries: scalar_subquery_in_expr, nested_plans_in_cte.\n\
  Hard caps (defaults): {'max_joins_default': 8, 'max_select_default': 200, 'max_predicates_default':\
  \ 200, 'max_limit_clamp_default': 1000}.\nOut of scope for NL→plan→SQL path: mutating\
  \ statements (INSERT/UPDATE/DELETE/DDL); arbitrary raw SQL strings from the LLM;\
  \ queries referencing tables/columns not in schema.yaml"
read_sql_surface_version: 1
plan_construction_procedure: "1) Choose dataset: the ONE table that contains the needed\
  \ information.\n2) Extract filters: convert ALL constraints to filter objects using\
  \ logical field names.\n   Use op='contains' for fuzzy text matching (building names,\
  \ keywords, etc.).\n   IMPORTANT: if the question mentions a time period ('last\
  \ year', 'this year',\n   'past month', 'last 30 days', 'in 2025', etc.) you MUST\
  \ add date filters\n   on the date column. NEVER omit date filters when a time period\
  \ is stated.\n   Use $relative_date sentinels (see Semantics rules) for dynamic\
  \ dates.\n3) Decide output shape:\n   - List of entities: dimensions + optional\
  \ metrics.\n   - Single scalar: metrics only (no dimensions), or dimensions + rollup.\n\
  4) Decide grouping:\n   - If question says 'by X' / 'per X' / 'for each X' / 'total\
  \ for each X', put X in dimensions.\n   - CRITICAL: if the question names multiple\
  \ specific values of a column (e.g. 'for building A, building B, building C') AND\
  \ asks for 'a total for each' / 'count per' / 'breakdown', you MUST include that\
  \ column in dimensions so the result has one row per value. Also use op='in' with\
  \ the list of values as the filter.\n5) Decide metrics: use exactly the aggregations\
  \ requested. Always give each metric an alias.\n6) Two-step aggregation (e.g. 'average\
  \ per building'):\n   - Inner: group by dimension, compute per-group metric with\
  \ alias.\n   - Rollup: compute avg/stddev/etc over that INNER metric alias.\n  \
  \ - OMIT limit from inner plan so all groups are included.\n   - rollup.limit=1\
  \ for scalar result.\n7) Order + limit: for 'top N' set order_by metric alias desc\
  \ + limit=N.\n8) Multi-CTE questions: the outer query's `dataset` should be the\
  \ CTE name that produces the rows you return (e.g. listing CTE), NOT the base table\
  \ again with empty filters — or repeat the same filters/`where` on the outer query.\n"
semantics_rules: "- Use 'contains' for fuzzy name/keyword matching (compiled as ILIKE\
  \ %...%).\n- Use 'is_not_null' when excluding missing values is necessary for correctness.\n\
  - Do NOT use avg(*) or avg(null) — avg/sum/min/max require a real column field.\n\
  - For time-based filters use the $relative_date sentinel (never a SQL expression):\n\
  \    last 7 days:  {\"$relative_date\": {\"op\": \"now_minus_days\", \"days\": 7}}\n\
  \    last 24 hrs:  {\"$relative_date\": {\"op\": \"now_minus_hours\", \"hours\"\
  : 24}}\n    today:        {\"$relative_date\": {\"op\": \"today\"}}\n    last calendar\
  \ year (two filters on the date column, AND):\n      >= {\"$relative_date\": {\"\
  op\": \"calendar_year_start\", \"year_offset\": -1}}\n      <  {\"$relative_date\"\
  : {\"op\": \"calendar_year_start\", \"year_offset\": 0}}\n- Free-text keyword searches:\
  \ OR \"contains\" across all plausible string columns\n  for that table from schema_summary\
  \ — do not use only one column unless the question names it.\n- If the schema declares\
  \ enum_columns, do NOT put free-text search terms into those columns;\n  they hold\
  \ categorical values, not keywords.\n- Table 'frpm' declares keyword_search_or [county_name,\
  \ district_name, school_name]: use advanced `where.or` of `contains` on those columns\
  \ for keyword search; legacy filters are ANDed.\n- For 'satscores': when counting\
  \ distinct satscores, use count_distinct('cds') — 'cds' is the primary identifier.\n\
  - Table 'satscores' declares keyword_search_or [sname, dname, cname]: use advanced\
  \ `where.or` of `contains` on those columns for keyword search; legacy filters are\
  \ ANDed.\n- Table 'schools' declares keyword_search_or [edopsname, eilname, admfname1,\
  \ admlname1]: use advanced `where.or` of `contains` on those columns for keyword\
  \ search; legacy filters are ANDed."
legacy_queryplan_format:
  description: Use this format for all standard analytics queries. Easier for LLMs
    and covers the vast majority of use cases.
  shape:
    version: '1.0'
    dataset: <table name from schema_summary>
    dimensions:
    - field: <logical_column>
      alias: <string>
    metrics:
    - agg: count|count_distinct|sum|avg|min|max
      field: <logical_column_or_*>
      alias: <string>
    filters:
    - field: <logical_column>
      op: <operator>
      value: <any or null>
    order_by:
    - by: <dimension_alias_or_metric_alias>
      dir: asc|desc
    limit: <int>
    offset: <int>
    rollup: <optional — see rollup block>
rollup:
  description: 'Use rollup for aggregate-of-aggregates: ''average per X'', ''stddev
    per X'', ''total of totals'', etc. The inner query groups and computes per-group
    values. The rollup outer query aggregates those values.'
  shape:
    metrics:
    - agg: avg|sum|min|max|count|count_distinct|stddev|variance
      field: <inner_alias>
      alias: <string>
    dimensions: <optional outer grouping>
    filters: <optional filters on inner outputs>
    order_by: <optional>
    limit: <int — use 1 for scalar result>
    offset: <int>
  critical: rollup.metrics[*].field MUST be an alias from the inner plan (a metric
    alias or dimension alias), NOT a raw dataset column name.
validation_checklist: 'Before outputting JSON, verify:

  - dataset is a table name from schema_summary

  - every dimension has {field, alias}

  - every metric has {agg, field, alias} with unique aliases

  - no metric uses avg/sum/min/max with field=''*'' or field=null

  - order_by.by references a dimension alias or metric alias

  - limit and offset are present

  - if rollup: rollup.metrics is non-empty, rollup.metrics[*].field references an
  inner alias

  '
examples:
- question: How many satscores are there in total?
  plan:
    version: '1.0'
    dataset: satscores
    dimensions: []
    metrics:
    - agg: count_distinct
      field: cds
      alias: total_satscores
    filters: []
    order_by: []
    limit: 1
    offset: 0
- question: Show me frpm where cdscode contains 'example'
  plan:
    version: '1.0'
    dataset: frpm
    dimensions: []
    metrics: []
    filters:
    - field: cdscode
      op: contains
      value: example
    order_by: []
    limit: 100
    offset: 0
- question: How many satscores per rtype?
  plan:
    version: '1.0'
    dataset: satscores
    dimensions:
    - field: rtype
      alias: rtype
    metrics:
    - agg: count_distinct
      field: cds
      alias: satscores_count
    filters: []
    order_by:
    - by: satscores_count
      dir: desc
    limit: 100
    offset: 0
- question: How many satscores for value_A, value_B, value_C? Give a total for each.
  note: When the user lists specific values and asks 'for each' / 'total for each',
    use op='in' with the list AND include the column in dimensions so the result has
    one row per value.
  plan:
    version: '1.0'
    dataset: satscores
    dimensions:
    - field: rtype
      alias: rtype
    metrics:
    - agg: count_distinct
      field: cds
      alias: total_satscores
    filters:
    - field: rtype
      op: in
      value:
      - value_A
      - value_B
      - value_C
    order_by:
    - by: total_satscores
      dir: desc
    limit: 100
    offset: 0
- question: What is the average number of satscores per rtype?
  note: 'Two-step rollup: inner groups by dimension, outer computes avg over inner
    alias.'
  plan:
    version: '1.0'
    dataset: satscores
    dimensions:
    - field: rtype
      alias: rtype
    metrics:
    - agg: count_distinct
      field: cds
      alias: satscores_per_rtype
    filters:
    - field: rtype
      op: is_not_null
      value: null
    order_by: []
    offset: 0
    rollup:
      metrics:
      - agg: avg
        field: satscores_per_rtype
        alias: avg_satscores_per_rtype
      limit: 1
      offset: 0
- question: How many frpm matching a free-text keyword last year (search across ['county_name',
    'district_name', 'school_name'] with OR, not AND on one column)?
  note: 'Table ''frpm'' has keyword_search_or [''county_name'', ''district_name'',
    ''school_name'']: use legacy filters for other constraints and a top-level `where`
    with `or` of `cmp` contains for the keyword across ALL listed columns.'
  plan:
    version: '1.0'
    dataset: frpm
    dimensions: []
    metrics:
    - agg: count_distinct
      field: id
      alias: total_frpm
    filters:
    - field: cdscode
      op: contains
      value: EXAMPLE VALUE
    where:
      or: &id001
      - cmp:
          left:
            col: county_name
          op: contains
          right: keyword
      - cmp:
          left:
            col: district_name
          op: contains
          right: keyword
      - cmp:
          left:
            col: school_name
          op: contains
          right: keyword
    order_by: []
    limit: 1
    offset: 0
- question: How many frpm matching 'keyword' for value_A, value_B, value_C of cdscode?
    Give a total for each.
  note: Combines keyword_search_or with a multi-value IN filter. Use op='in' on cdscode
    AND include it in dimensions, plus where.or for the keyword search.
  plan:
    version: '1.0'
    dataset: frpm
    dimensions:
    - field: cdscode
      alias: cdscode
    metrics:
    - agg: count_distinct
      field: id
      alias: total_frpm
    filters:
    - field: cdscode
      op: in
      value:
      - value_A
      - value_B
      - value_C
    where:
      or: *id001
    order_by:
    - by: total_frpm
      dir: desc
    limit: 100
    offset: 0
